package siilinkari.types

/**
 * Represents the types supported in the language.
 *
 * Since we have a fixed set of non-parameterizable types,
 * a simple enum will do for now.
 */
enum class Type {
    String, Int, Boolean
}
